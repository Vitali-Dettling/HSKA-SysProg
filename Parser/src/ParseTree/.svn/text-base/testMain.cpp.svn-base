/*
 * testMain.cpp
 *
 *  Created on: May 28, 2012
 *      Author: devi1013
 */

#include "IParseTree.h"
#include "MakeCodeOutput.h"
#include "Node.h"
#include "../../../SymbolTable/src/Container/Info.h"
#include "../../../Scanner/src/TType.h"
#include <iostream>

using namespace std;

int main()
{/*
	String* s = new String();
	s->add( new char('4') );
	s->add( new char('2') );
	s->calcValue();
	Info* i = new Info( INTEGER , 42 , s);

	Node* n = new Node( ROOT  , NULL);
	Node* root = n;

	MakeCodeOutput* mc = new MakeCodeOutput( n );
	TypeCheck* tc = new TypeCheck( n );

	//printf("\n");
	//cout << "Überprüft ob der Zahlenwert dassächlich noch als Sting vorhanden ist." << endl;
	//cout << "String: " << s->getString() << " Zahlenwert: " << s->getValue() << endl;

	printf("\n");
	cout << "Einfügen eines Knotens"  << endl;
	n = n->add( PROG , NULL );
	n = n->add( DECLS_1 ,NULL );
	n->add( DECL , i );
	cout << "Überprüfen des Knotens -> typeCheck()" << endl;
	//root->typeCheck(tc);
	cout << "Erstellt den Code ->makeCode" << endl;
	n->makeCode( mc );



	cout << "Erfolgreich" << endl;

return 0;
*/
}

